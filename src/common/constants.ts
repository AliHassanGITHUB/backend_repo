// Default password assigned to every newly approved citizen account.
// The admin delivers it to the citizen in person (or it is shown on the
// track-registration screen) — citizens are expected to change it after login.
export const DEFAULT_CITIZEN_PASSWORD = 'citizen.100';
